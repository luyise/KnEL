Variables = {
  X : Type,
  Y : Type,
  P : X → Y → Type
}

Theorem Choice :
    (Π (x : X), Σ (y : Y), P x y)
  → (Σ (f : X → Y), Π (x : X), P x (f x))
Proof
  Intro H;
  Split (λ (x : X) → fst (H x));
Ongoing